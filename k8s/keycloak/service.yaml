apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: auth-platform
  labels:
    app.kubernetes.io/name: keycloak
    app.kubernetes.io/part-of: auth-platform
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/name: keycloak
