apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: auth-platform
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: auth-platform
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/name: frontend
